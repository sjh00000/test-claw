spring:
  application:
    name: openclaw-review-admin
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite:${REVIEW_ADMIN_DB_PATH:C:\Users\LSYM005169\.openclaw\skills\openclaw-commit-review\runtime\db\review-log.sqlite}

server:
  port: 8080

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true
