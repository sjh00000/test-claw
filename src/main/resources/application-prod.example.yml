spring:
  datasource:
    # prod 在 Docker 网络内通过服务名访问 MySQL；账号密码要与 .env 中的 MySQL 初始化参数保持一致。
    url: jdbc:mysql://mysql:3306/keyframe_video_studio?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: kvstudio
    password: change-me-db-password
  data:
    redis:
      # prod 在 Docker 网络内通过服务名访问 Redis。
      host: redis
      port: 6379
      password:
      database: 0
      timeout: 2000ms
